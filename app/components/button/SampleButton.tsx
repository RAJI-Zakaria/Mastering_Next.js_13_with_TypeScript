'use client'
import React from 'react'

const SampleButton = () => {
  return (
    <button className="btn btn-primary" onClick={()=>{alert("under construction")}}>Get Started</button>
  )
}

export default SampleButton