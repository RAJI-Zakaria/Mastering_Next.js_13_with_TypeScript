'use client'
import React from 'react'

const AddToCart = () => {
  return (
    <button onClick={() => alert('product added successfully')}>
      Add to cart
    </button>
  )
}

export default AddToCart
